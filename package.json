{
    "name": "chat-app",
    "version": "1.0.0",
    "scripts": {
        "first-setup": "npm install && npm run setup",
        "setup": "cd chat-backend && npm install && npm run setup-db && cd ../chat-frontend && npm install",
        "setup-db": "node setup_db.js",
        "start-all": "npm run start-backend & npm run start-frontend",
        "start-backend": "cd chat-backend && npm start",
        "start-frontend": "cd chat-frontend && npm start"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
