{
    "name": "chat-app",
    "version": "1.0.0",
    "scripts": {
        "setup": "cd chat-backend && npm run setup && cd ../chat-frontend && npm install",
        "start-backend": "cd chat-backend && npm start",
        "start-frontend": "cd chat-frontend && npm start",
        "start-all":  "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
        "first-setup": "npm install && npm run setup && npm run start-all"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}
